apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fbi-crime-analyst-agent.fullname" . }}-config
  labels:
    {{- include "fbi-crime-analyst-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
